name: Frequent News Sync

on:
  schedule:
    - cron: '22,44 * * * *' # Runs twice an hour
  workflow_dispatch: # Allows manual runs

# --- PREVENTS DEPLOYMENT OVERLAP ---
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Needed to push news_data.json
      pages: write     # Needed for GitHub Pages
      id-token: write  # Needed for security/OIDC

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip' # Speeds up installation

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install httpx beautifulsoup4

      - name: Run News Scraper
        run: python update_news.py

      - name: Commit and Push changes
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          # Only adds existing files to avoid creating extra junk
          git add news_data.json || echo "Files not found"
          git commit -m "Auto-update news data" || echo "No changes to commit"
          git push
